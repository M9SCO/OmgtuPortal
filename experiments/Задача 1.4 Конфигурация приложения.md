# Задача

Задача 1.4. Конфигурация приложения
Описание: все переменные окружения из .env централизуются в типизированном классе AppSettings, регистрируемом как singleton через DI. Контроллеры и  сервисы получают конфигурацию через DI, не обращаясь к  Environment.GetEnvironmentVariable напрямую..
Критерии приёмки:
- Класс AppSettings содержит все переменные из .env.
- AppSettings зарегистрирован через DI как singleton.
- Program.cs не содержит прямых вызовов Environment.GetEnvironmentVariable.
- При отсутствии обязательной переменной приложение завершается с понятным сообщением об ошибке.


> `Agent`: Explore codebase structure


**Агент:**

Проект пока не имеет `.env` файла и не использует `Environment.GetEnvironmentVariable`. Создам инфраструктуру конфигурации с нуля.


> `Bash`: dotnet add package DotNetEnv --version 3.1.1


> `Read`: {'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/Program.cs'}


> `tool_result`: info : X.509 certificate chain validation will use the fallback certificate bundle at '/usr/share/dotnet/sdk/10.0.104/trustedroots/codesignctl.pem'.
info : X.509 certificate chain validation will use the fallback certificate bundle at '/usr/share/dotnet/sdk/10.0.104/trustedroots/timestampctl.pem'.
i


> `tool_result`: 1→using Microsoft.Extensions.FileProviders;
     2→
     3→var builder = WebApplication.CreateBuilder(args);
     4→
     5→var app = builder.Build();
     6→
     7→var distPath = Path.Combine(app.Environment.ContentRootPath, ".frontend", "dist");
     8→
     9→if (Directory.Exists(distPath))
    


> `Bash`: ls /home/kunzz/RiderProjects/OmgtuPortal/


> `tool_result`: appsettings.Development.json
appsettings.json
bin
Controllers
experiments
obj
OmgtuPortal.csproj
OmgtuPortal.http
parser.py
Program.cs
Properties


**Агент:**

Now I'll create all the files. Since the project is early-stage, I'll define a minimal set of environment variables that a university portal will need (DB connection, JWT secret).


> `Write`: {'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/.env.example', 'content': 'DATABASE_URL=Host=localhost;Port=5432;Database=omgtu_portal;Username=postgres;Password=secret\nJWT_SECRET=change-me-to-a


> `Write`: {'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/Configuration/AppSettings.cs', 'content': 'namespace OmgtuPortal.Configuration;\n\npublic class AppSettings\n{\n    public required string Database


> `tool_result`: File created successfully at: /home/kunzz/RiderProjects/OmgtuPortal/.env.example


> `tool_result`: File created successfully at: /home/kunzz/RiderProjects/OmgtuPortal/Configuration/AppSettings.cs


> `Write`: {'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/Program.cs', 'content': 'using Microsoft.Extensions.FileProviders;\nusing OmgtuPortal.Configuration;\n\nDotNetEnv.Env.Load();\n\nvar builder = WebA


> `tool_result`: The file /home/kunzz/RiderProjects/OmgtuPortal/Program.cs has been updated successfully.


**Агент:**

Let me verify it compiles, and also create a `.env` for local development so the app can start.


> `Bash`: cp /home/kunzz/RiderProjects/OmgtuPortal/.env.example /home/kunzz/RiderProjects/OmgtuPortal/.env


> `Bash`: dotnet build 2>&1


> `tool_result`: Determining projects to restore...
  All projects are up-to-date for restore.
  OmgtuPortal -> /home/kunzz/RiderProjects/OmgtuPortal/bin/Debug/net10.0/OmgtuPortal.dll

Build succeeded.
    0 Warning(s)
    0 Error(s)

Time Elapsed 00:00:04.58


**Агент:**

Сборка успешна. Проверю также, что при отсутствии переменных приложение падает с понятным сообщением.


> `Bash`: DATABASE_URL="" JWT_SECRET="" dotnet run --no-build 2>&1 | head -20


> `tool_result`: Command running in background with ID: bw82nx41n. Output is being written to: /tmp/claude-1000/-home-kunzz-RiderProjects-OmgtuPortal/tasks/bw82nx41n.output


> `TaskOutput`: {'task_id': 'bw82nx41n', 'block': True, 'timeout': 10000}


> `tool_result`: <retrieval_status>timeout</retrieval_status>

<task_id>bw82nx41n</task_id>

<task_type>local_bash</task_type>

<status>running</status>


> `TaskStop`: {'task_id': 'bw82nx41n'}


> `tool_result`: {"message":"Successfully stopped task: bw82nx41n (DATABASE_URL=\"\" JWT_SECRET=\"\" dotnet run --no-build 2>&1 | head -20)","task_id":"bw82nx41n","task_type":"local_bash","command":"DATABASE_URL=\"\" JWT_SECRET=\"\" dotnet run --no-build 2>&1 | head -20"}


> `TaskOutput`: {'task_id': 'bw82nx41n', 'block': False, 'timeout': 1000}


> `tool_result`: <tool_use_error>No task found with ID: bw82nx41n</tool_use_error>


> `Bash`: mv .env .env.bak && dotnet run --no-build 2>&1; EXIT=$?; mv .env.bak .env; exit $EXIT


> `tool_result`: Exit code 134
Using launch settings from /home/kunzz/RiderProjects/OmgtuPortal/Properties/launchSettings.json...
Unhandled exception. System.InvalidOperationException: Missing required environment variables: DATABASE_URL, JWT_SECRET. Copy .env.example to .env and fill in the values.
   at OmgtuPorta


**Агент:**

Все критерии выполнены. Вот что сделано:

**Созданные/изменённые файлы:**

1. **`Configuration/AppSettings.cs`** — типизированный класс с `required`-свойствами (`DatabaseUrl`, `JwtSecret`). Статический метод `Load()` читает переменные окружения и при отсутствии обязательных выбрасывает `InvalidOperationException` с перечислением недостающих переменных.

2. **`Program.cs`** — загружает `.env` через `DotNetEnv.Env.Load()`, создаёт и регистрирует `AppSettings.Load()` как singleton через `builder.Services.AddSingleton(...)`. Прямых вызовов `Environment.GetEnvironmentVariable` в `Program.cs` нет.

3. **`.env.example`** — шаблон с переменными `DATABASE_URL` и `JWT_SECRET` (отслеживается git).

4. **`.env`** — локальная копия (игнорируется git).

5. **`OmgtuPortal.csproj`** — добавлен пакет `DotNetEnv 3.1.1`.

**Проверка:** при отсутствии `.env` приложение падает с сообщением:
```
Missing required environment variables: DATABASE_URL, JWT_SECRET.
Copy .env.example to .env and fill in the values.
```


---

**Токены:** input: 26 | cache_create: 39,448 | cache_read: 604,933 | output: 3,307 | **стоимость: 0.13 у.е.**