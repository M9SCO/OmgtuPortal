# Задача

Сделай чтобы ASP.NET Core отдавал фронтенд из .frontend/dist по всем маршрутам кроме /api.


> `Agent`: Explore project structure


> `Read`: {'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/Program.cs'}


> `tool_result`: 1→var builder = WebApplication.CreateBuilder(args);
     2→
     3→builder.Services.AddOpenApi();
     4→
     5→var app = builder.Build();
     6→
     7→if (app.Environment.IsDevelopment())
     8→{
     9→    app.MapOpenApi();
    10→}
    11→
    12→app.MapGet("/api/health", () => Results.Ok(new


> `Edit`: {'replace_all': False, 'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/Program.cs', 'old_string': 'var builder = WebApplication.CreateBuilder(args);\n\nbuilder.Services.AddOpenApi();\n\nvar app = 


> `tool_result`: The file /home/kunzz/RiderProjects/OmgtuPortal/Program.cs has been updated successfully.


**Агент:**

Теперь добавлю нужный `using`:


> `Edit`: {'replace_all': False, 'file_path': '/home/kunzz/RiderProjects/OmgtuPortal/Program.cs', 'old_string': 'var builder = WebApplication.CreateBuilder(args);', 'new_string': 'using Microsoft.Extensions.Fil


> `tool_result`: The file /home/kunzz/RiderProjects/OmgtuPortal/Program.cs has been updated successfully.


**Агент:**

Проверю, что собирается:


> `Bash`: dotnet build 2>&1 | tail -5


> `tool_result`: Build succeeded.
    0 Warning(s)
    0 Error(s)

Time Elapsed 00:00:04.15


**Агент:**

Готово. Вот что изменилось в `Program.cs`:

1. **`UseStaticFiles`** — отдаёт статику (JS, CSS, картинки) из `.frontend/dist`
2. **`MapFallback`** — все маршруты, которые не совпали с `/api/*` и не являются статическими файлами, отдают `index.html` (SPA fallback). Запросы к `/api/*` без совпавшего маршрута получат 404.

Не забудь собрать фронтенд перед запуском:

```bash
cd .frontend && pnpm build
```


---

**Токены:** input: 13 | cache_create: 7,193 | cache_read: 195,249 | output: 1,256 | **стоимость: 0.03 у.е.**